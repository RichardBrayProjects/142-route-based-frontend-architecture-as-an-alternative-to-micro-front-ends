{
  "name": "@toy/tailwind-config",
  "version": "1.0.0",
  "private": true,
  "main": "tailwind.config.js",
  "scripts": {
    "build": "echo tailwind config ready",
    "package-cleanup": "rm -rf node_modules dist"
  }
}
